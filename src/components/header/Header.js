import './header.scss';

const Header = () => {
    return (
        <header className="weather-header">
            <h1 className="weather-header__heading">Weather App</h1>
        </header>
    );
}

export default Header;